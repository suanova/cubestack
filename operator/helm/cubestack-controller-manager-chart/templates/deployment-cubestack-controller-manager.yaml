# Generated by hack/update-helm-resources.sh — DO NOT EDIT
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: cubestack
    control-plane: controller-manager
  name: cubestack-controller-manager
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cubestack
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: cubestack
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        {{- with .Values.gateway.name }}
        - --gateway-name={{ . }}
        {{- end }}
        - --gateway-namespace={{ .Release.Namespace }}
        {{- with .Values.gateway.dataplaneNamespace }}
        - --gateway-dataplane-namespace={{ . }}
        {{- end }}
        {{- if .Values.gateway.catalogHostname }}
        - --gateway-catalog-hostname={{ .Values.gateway.catalogHostname }}
        {{- end }}
        - --l4-port-range-start={{ .Values.l4PortRange.start }}
        - --l4-port-range-end={{ .Values.l4PortRange.end }}
        - --rdma-ib-resource={{ .Values.rdma.ibResource }}
        - --rdma-roce-resource={{ .Values.rdma.roceResource }}
        command:
        - /manager
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 8081
          name: health
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts: []
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: cubestack-controller-manager
      terminationGracePeriodSeconds: 10
      volumes: []

